spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
#      name: ecommerce
      # 마이크로서비스명을 입력하여 config 파일을 읽을 수 있다. 대신에 application.yml파일을 읽어온다.
      name: config-service
#  profiles:
#    active: dev
